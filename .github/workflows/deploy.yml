name: Deply to Server

on: push

jobs:
  first_jobs:
    runs-on: ubuntu-latest

    env:
     NAME: codigoFacilito

    steps:
      - name: hola mundo
        run: echo "Hola Mundo linux $codigoFacilito"

  second_jobs:
    if: ${{github.ref == 'refs/heads/main'}}
    needs: first_jobs
    runs-on: windows-latest
    steps:
      - name: hola mundo windows
        run: echo "hola ${{github.actor}} desde windows"